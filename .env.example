# SQLite file, zero external setup — created automatically by `npm run setup`.
DATABASE_URL="file:./dev.db"

# Used to sign/verify session JWTs. Any long random string works for local
# dev; generate one with `openssl rand -base64 32` before deploying anywhere
# real.
JWT_SECRET="replace-with-a-long-random-string"
